# Hyperparameter and split protocol candidate

The existing 30 cases and seven engineering fixtures are exposed development material forever, even after human adjudication. Do not relabel eight of a 12-case pilot as calibration after observing it. Group common source, entity, template, paraphrase and copied origin across all splits. Freeze a split manifest before any comparative output.

1. Development: adjudicated existing cases plus explicitly assigned development-only cost dry runs. Resolve ontology and adapter defects; estimate feasible context and compute. These outcomes are not pilot evidence.
2. Pilot evaluation: new independently authored/adjudicated scenario families, never used for selecting thresholds/prompts. All repeated masks/seeds/policies stay within their family.
3. Future confirmation: new held-out domains/source families after pilot-driven sample-size planning; never retune on this set.

B4 lexical .70 is a fixture engineering setting, not a published optimized semantic baseline. Before pilot, compare a frozen lexical control and an accessible pinned embedding/model-neighbor implementation on development only. Candidate thresholds {.3,.5,.7,.9}; declare a guardrail-constrained task utility or report the full development frontier. Choose one threshold using prespecified tie-breaking (lower actual cost, then higher valid retention); log every trial. If no threshold meets preservation constraints, report that and retain the strongest feasible operating point rather than removing the baseline.

B5 and B7 must share provider, proposal and threshold. Candidate threshold grid {.3,.5,.7,.9}; probabilities are treated as scores unless separately calibrated. Give competing algorithms equal development search budgets. Cache settings, reasoning effort, verifier context, candidate ordering and inference prompts are hyperparameters too. A future uncertainty controller needs a separate calibration split and a defined event, but no such controller is authorized now.

Budgets are selected from development costs and operational constraints, without inspecting pilot policy ordering. Freeze one matched cap and an unrestricted reference, with sensitivity caps declared exploratory. The existing six-credit suggestion is not a validated optimum. Store selected config, all tried configs, rationale, date, commit and approver. Post-freeze bug fixes require versioned reruns of all paired affected conditions; changes to a method after seeing pilot results are exploratory and require new confirmation data.
