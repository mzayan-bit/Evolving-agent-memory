# Fair comparator matrix — candidate, not frozen, 2026-09-26

All ordinary arms receive projected visible records only. External evidence is disabled throughout this harness. `H` means common immutable archive plus existing fixed-proposition historical readout; it does not imply a model-backed historical reader. “Conditional” matched eligibility means accounting/plumbing exists but live validation and the final resource schedule are missing. No row uses confidence adaptively downstream. No ordinary row receives gold; explicit O-level variants must be separately labeled.

| Comparator | Persistent mutation | Model calls | Support structure | History | Matched eligibility | Provenance/fidelity |
|---|---|---|---|---|---|---|
| B0 no repair | No (common ingestion still occurs) | None | None | H | Deterministic only | Internal control |
| B1 source-only | Source invalidation | None | Changed-source cue | H | Deterministic only | Internal control |
| B2 full replay | Status refresh | None presently | Visible executable positive rules | H | Rule-work only | Internal full-recompute analogue, no text regeneration |
| B3 explicit closure | Descendant invalidation | None | Visible pairwise projection | H | Deterministic only | Rollback-related analogue, not reproduction |
| B4a lexical closure (legacy B4) | Neighbor invalidation | None | Word cosine, no support inference | H | Deterministic only | Weak internal control |
| B4b embedding closure | Neighbor invalidation | Local embedding encodes | Pinned semantic vectors, no entailment | H | Conditional; encode time/calls | Internal; backend live check skipped |
| B5a point graph | Descendant invalidation | Support inference + bounded schema retry | Frozen thresholded groups projected to arcs | H | Conditional | Internal model-backed adaptation |
| B5b point support sets | Support status refresh | Same proposal construction as B5a | Frozen AND/OR groups | H | Conditional | Internal model-backed classical composition |
| B6 conservative reverify | Checked status refresh | None presently; counters are symbolic | Visible executable rules | H | Rule-work only | Simplified analogue, not a real semantic verifier |
| B7 positive support maintenance | Support status refresh | None for supplied proposals | Positive single-context AND/OR fixed point | H | Conditional on inference charge | Internal, neither full ATMS nor JTMS reproduction |
| B8 cached replay | Incremental status refresh | None presently | Same executable rules as B2; ancestor cache | H | Rule-work only | Internal incremental computation analogue |
| B9 support-aware rollback | Descendant invalidation/rescue | None for supplied visible supports | AND/OR; valid source roots with known disjoint origins | H | Conditional on inference charge | Internal analogue, not official Rollback |
| B10 query-only audit | No | One paid verifier attempt per query; no reuse by default | Semantic current evidence audit | Current only | Conditional | Internal query-audit adaptation, not StateAuditor reproduction |
| CUPMem native | Native profile updates | Native pipeline, unrun here | Native profile/session semantics | Native task only | Excluded from direct G1 ranking | Official store component reproduced; incompatible direct mapping |
| Rollback native | Native replay | Not integrated | Native execution lineage | Not integrated | Excluded | No attributable adaptable official implementation located in prior audit |

The ordinary B7/B9 rule input can be replaced by the same frozen B5b point supports for policy-isolation comparisons. Record the construction ledger equally for all receiving arms. A supplied accurate fixture graph is not evidence of strong language inference. O-level complete/gold lineage is a diagnostic ceiling and never participates in non-oracle ranking. Authority/provenance access must be matched in addition to raw text. No model backend receives executable fixture rules through the prompt allowlist.

B10 currently audits full visible context without an external retrieval call; this gives it the same source pool and charges context tokens. Do not claim a tuned retrieval system. Record original stale storage, audited answer, verifier failure, repeated-query costs and subsequent unaudited reads separately. Historical query auditing and evidence selection are unresolved integration work, so B10 is not silently included in all historical outcomes.

Strongest structural threat: **B5b plus classical support maintenance**, because accurate point AND/OR inference already supplies the central repair logic. B8 and B10 remain potentially decisive operational threats. There is no empirical ranking from mocks or fixtures.
